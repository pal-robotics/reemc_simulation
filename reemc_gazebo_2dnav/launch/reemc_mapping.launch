<?xml version="1.0" encoding="UTF-8"?>

<!--
Usage:
roslaunch reemc_gazebo_2dnav reemc_mapping.launch
Rotate the robot manually in gazebo and walk with (set the desired #steps):
roslaunch walking walk.launch nsteps:=4
-->
<launch>
  <!-- Simulation time -->
  <param name="/use_sim_time" type="bool" value="true"/>

  <arg name="mapping" default="gmapping"/>
  <arg name="odometry" default="controller"/>
  <arg name="world" default="small_office"/>
  <arg name="robot" default="full"/>
  <arg name="gzname" default="gazebo"/>
  <arg name="gzpose" default="-x 0.0 -y 0.0 -z 0.0 -R 0.0 -P 0.0 -Y 0.0"/>

  <arg name="fuerte" default="true"/>

  <group ns="reemc_sim">

    <include file="$(find reemc_2dnav)/launch/mapping.launch">
      <arg name="mapping" value="$(arg mapping)"/>
      <arg name="odometry" value="$(arg odometry)"/>
    </include>

    <include file="$(find reemc_gazebo)/launch/reemc_gazebo.launch">
      <arg name="world" value="$(arg world)"/>
      <arg name="robot" value="$(arg robot)"/>
      <arg name="gzname" value="$(arg gzname)"/>
      <arg name="gzpose" value="$(arg gzpose)"/>
    </include>

    <include file="$(find reemc_controller_configuration)/launch/walking_controller.launch"/>

    <node name="rviz" pkg="rviz" type="rviz" args="-d $(find reemc_gazebo_2dnav)/config/reemc_mapping.vcg" if="$(arg fuerte)"/>
    <!--node name="rviz" pkg="rviz" type="rviz" args="-d $(find reemc_gazebo_2dnav)/config/reemc_mapping.rviz" unless="$(arg fuerte)"/-->
    <node name="rviz" pkg="rviz" type="rviz" unless="$(arg fuerte)"/>

  </group>
</launch>
